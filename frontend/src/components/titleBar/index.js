import React from 'react';
import styled from 'styled-components';

const Header = styled.h1`
  color: #2B4570;
  font-size: 50px;
  margin: 20px
`
const TitleBar = <Header> MeetDown :) </Header>;

export default TitleBar;
